name: quick-pulse
description: Fast pulsing sine wave — higher cadence
base_period: 1.0
layers:
  - name: base
    blend: set
    axes:
      V0: { waveform: sine, freq_multiple: 1.0, center: 0.45, amplitude: 0.22 }
