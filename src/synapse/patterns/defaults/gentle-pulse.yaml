name: gentle-pulse
description: Slow, soft sine wave — good starting point
base_period: 4.0
layers:
  - name: base
    blend: set
    axes:
      V0: { waveform: sine, freq_multiple: 1.0, center: 0.3, amplitude: 0.15 }
