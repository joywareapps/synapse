name: "starter-pulse-fast"
description: "Faster pulse sweep for comparison"
base_period: 2.0
layers:
  - name: base
    blend: set
    axes:
      V0: { waveform: hold, center: 0.4, amplitude: 0.0 }
      P0: { waveform: triangle, freq_multiple: 1.0, center: 0.5, amplitude: 0.4 }
